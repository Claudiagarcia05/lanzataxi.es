<?php

	namespace App\Http\Controllers\Api;

	use App\Http\Controllers\Controller;

	/**
	 * Geocodificación (placeholder).
	 *
	 * Este controlador está vacío actualmente. Si se implementa:
	 * - Convertir dirección -> coordenadas (geocoding)
	 * - Convertir coordenadas -> dirección (reverse geocoding)
	 *
	 * Idealmente se debe integrar con un proveedor (Google, Mapbox, Nominatim, etc.)
	 * y proteger con rate limiting/cache.
	 */
	class GeocodificacionController extends Controller {

	}